import { createAction } from 'redux-actions'

export const parsePageAction = createAction('PARSE_PAGE')
export const finishParseAction = createAction('PARSE_FINISH')
export const parseAllClassesAction = createAction('PARSE_ALL_CLASSES')
